import numpy as np

var1 = np.array([(2, 4, 6), (1, 3, 5)])

var2 = np.array([(7, 8, 9), (4, 2, 1)])

# print(var1+var2)
#
# print(var2.ravel())  # ravel-Printing the all the data
#
# print(var1.sum(axis=0))  # axis-Calculate the numbers in the () itself
#
# print(np.sqrt(var2))

print(np.std(var1)) # Standard deviation
